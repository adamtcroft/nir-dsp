1. Keep improving the frequency response graph GUI in `plugins/BiquadLowPassGraphPrototype.jsfx` (pick ONE tiny item per run).
   - Match the overall feel of `docs/Examples/pro-q-4-highlight-spectral-poster.jpg` (colors/alpha, contrast, legibility).
   - Add subtle inner highlight/edge treatment to make the graph panel feel less flat.
   - Improve Y-axis label vertical centering/clamping so the top/bottom labels never clip.
   - Make the cutoff handle feel more "Pro-Q-ish" (ring thickness, glow falloff, contrast against grid).
2. After refining the frequency graph and having approval (approval will show up in this document), archive old Low Pass Filter and make the Graph Prototype called "Low Pass Filter (NIR)".
3. After low pass filter is approved, make a matching High Pass Filter.
